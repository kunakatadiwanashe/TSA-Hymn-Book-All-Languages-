# Enable Vertical Scrolling in the App

## Tasks

- [ ] Add flex: 1 to the container View in app/index.tsx to allow FlatList to scroll vertically when content overflows.
- [ ] Add flex: 1 to the ScrollView in app/hymn.tsx to enable vertical scrolling for hymn content.
- [ ] Add flex: 1 to the container View in app/favorites.tsx to allow FlatList to scroll vertically when content overflows.
- [ ] Add flex: 1 to the container View in app/favorites.tsx to allow FlatList to scroll vertically when content overflows.
